<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.5.xsd">

    <changeSet id="1613158" author="dn021299baa" runOnChange="true">
        <sql>
            CREATE TABLE if not exists structural.revinfo
            (
                revision_number bigint,
                revision_timestamp timestamp without time zone not null,
                CONSTRAINT revinfo_pkey PRIMARY KEY (revision_number)
            );

            COMMENT ON TABLE structural.revinfo IS 'Дані аудиту';
            ALTER TABLE structural.revinfo OWNER TO postgres;
            GRANT SELECT,UPDATE,DELETE,INSERT ON structural.revinfo TO postgres;
        </sql>
    </changeSet>

</databaseChangeLog>
